analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Comments were deliberately stripped from lib/; this project does not
    # document public members with doc comments.
    public_member_api_docs: false
